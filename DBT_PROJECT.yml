
name: 'CUSTOMER_SUBSCRIPTION_PROJECT'
version: '1.0.0'
config-version: 2

profile: 'CUSTOMER_SUBSCRIPTION_PROJECT'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
CUSTOMER_SUBSCRIPTION_PROJECT:
    +materialized: view
